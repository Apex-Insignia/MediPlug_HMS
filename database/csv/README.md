# CSV Seed Data

> **IMPORTANT: Please refer to `/database/README.md` (one level up) for the final, exact step-by-step Supabase database setup and CSV import order.**

The instructions that used to be here were outdated. The final schema and exact import mappings have been finalized and documented in the main `database/README.md` and `database/data_dictionary.md`.
